import { IDTypes, whitespaces, validator } from "./constants";

/**
 * 
 * Decode data
 * @param {string} input Data to decode
 * @returns {IDTypes}
 */
export function decode (input: string): IDTypes {
    return '';
}